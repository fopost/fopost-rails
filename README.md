# FoPost for Rails

Official Rails integration for the [FoPost](https://fopost.com) API.

A thin wrapper around the [`fopost`](https://github.com/fopost/fopost-ruby) gem: it wires the SDK
into Rails configuration, ActiveJob, and a mountable webhook endpoint.

MIT licensed.
